/** Shared $5 unlock pitch — keep buy surfaces in sync. */
export const UNLOCK_PRICE_LABEL = "$5";

export const UNLOCK_FEATURES_SHORT =
  "all games, Google Calendar export, and no ads";

export const UNLOCK_FEATURES_SENTENCE = `${UNLOCK_PRICE_LABEL} unlocks all festival games, Google Calendar export for My sets, and removes ads.`;

export const UNLOCK_CTA_FULL = `Unlock all games + calendar — ${UNLOCK_PRICE_LABEL}`;

export const FREE_SCHEDULE_FEATURES =
  "Star sets for free in-app alerts while this tab is open.";

export const PREMIUM_SCHEDULE_FEATURES =
  "Google Calendar download & import requires unlock.";
